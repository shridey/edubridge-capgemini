export class Profile {
    constructor(public profileId: number, public realname: string, public bio: string, public dob: string, public avatarUrl: string, public postCount: number, public followersCount: number, public followingCount: number) {}
}
